const SIGNIN = "/signin";
const SIGNUP = "/signup";
const TODOS = "/todos";
const HOME = "/";

const router = {
  home: HOME,
  signin: SIGNIN,
  signup: SIGNUP,
  todos: TODOS,
};

export default router;
